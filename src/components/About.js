

function About() {

    return (
        <div className="about content">
            <h1 className="title">About The Tool</h1>
            <h3 className="body ">This app is designed to streamline the comparison process by taking in one main input and two secondary inputs. The main input serves as the basis for comparison, while the secondary inputs are evaluated against it. </h3>
            <p className="body">Currently the comparison guide only works for exact item numbers. The Select Item dropdown menu can be used to locate the item numbers for that particular product line. </p>
        </div>
    )
}

export default About;